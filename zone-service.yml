server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/agms_zone_db
    username: postgres
    password: Hasinduand12345
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/